# 오늘 할일 - 2026년 4월 24일

## 요약

1. 어제 올라온 이슈 #265 (HWP 3.0 파일 열기 실패) 를 5-Stage 로 해결. 표면적으로는 "감지 + 친절한 에러 메시지" 1건 작업이었으나, 끝까지 파고들며 2건의 구조적 버그를 함께 발견·수정.
2. 작업지시자 제보로 #275 (bitmap.hwp/한셀OLE.hwp 빈 페이지 렌더) 를 3-Stage 로 해결. WASM canvas 렌더러가 OLE → `RawSvg`/`Placeholder` 렌더 노드를 처리하지 않아 암묵 무시되던 구조 버그. `svg.rs` 와 `web_canvas.rs` 사이의 match arm 비대칭이 원인.

오후엔 `exam_math.hwp` p.7 #18 "수열" 문항 시각 불일치 제보 받아 이슈 #290 등록 + 4-Stage 로 해결 (cross-run 탭 감지가 inline_tabs 를 무시하여 LEFT 탭을 RIGHT 오판하는 경계 버그).

## 1. Task #265 — HWP 3.0 파일 감지 + 친절한 에러 메시지

### 배경

@jangster77 님의 제보 (2026-04-23). `samples/issue_265.hwp` 를 rhwp 의 어떤 경로에서도 열지 못함. 원인: **HWP 3.0** 바이너리 포맷 (1996~1998년 경 한글 3.0). 현재 rhwp 는 HWP 5.0 + HWPX 만 지원.

### 처리 방침
작업지시자 지시: "HWP 3.0 정식 지원은 이번 마일스톤 범위 아님. 감지 + 친절한 에러 메시지까지만".

### 변경 범위
- **코드** `src/parser/mod.rs`:
  - `FileFormat::Hwp3` variant + `detect_format` 에 17바이트 프리픽스 체크
  - `ParseError::UnsupportedFormat { format, hint }` + `Display` 확장
  - `parse_document` 에서 Hwp3 → 한글 안내 메시지 반환
- **코드** `src/error.rs` — **발견된 추가 버그 수정**:
  - `From<ParseError> for HwpError` 등 3개 `From` 이 `{:?}` (Debug) 를 쓰고 있어 친절한 Display 메시지가 사용자에게 도달하지 못함. `{e}` (Display) 로 전환. CFB 매직 바이트 내부 누출도 함께 해결
- **코드** `rhwp-studio/src/main.ts` — **UI 전달 누락 수정**:
  - 기존: 상태 표시줄 (22px, `overflow: hidden`) 에만 쓰고 있어 긴 메시지 대부분 가려짐
  - 수정: `showLoadError` 유틸 + 우상단 토스트 (`showToast` 기존 유틸 재사용). 3 로드 경로 (`loadFile` · `loadFromUrlParam` · `open-document-bytes` 이벤트) 통합
  - `open-document-bytes` 이벤트 핸들러가 WASM throw 를 catch 하지 않아 "파일 열기" 커맨드 에선 에러가 완전 묵살되던 문제도 함께 수정
- **테스트** 6건 (detect + parse + Display 회귀 방어)
- **WASM 재빌드**: 4,076,166 → 4,081,046 bytes (+4,880 bytes)

### 검증
- `cargo test --lib`: **963 passed / 0 failed**
- `cargo test --test svg_snapshot`: 3 passed
- `cargo clippy --lib -- -D warnings`: clean
- `tsc --noEmit` (rhwp-studio): clean
- 회귀 스모크 (HWP 5.0 · HWPX 각 2건): 정상 렌더
- 제보자 파일 UX: 우상단 토스트로 "지원하지 않는 포맷입니다: HWP 3.0. ..." 정상 노출 (작업지시자 실측 확인)

### 절차적 개선 (#264 사건 교훈 적용)
- 이슈 착수 즉시 `gh issue edit --add-assignee` 실행 → 외부 기여자 중복 작업 원천 차단

### 산출물
- `mydocs/plans/task_m100_265{,_impl}.md`
- `mydocs/working/task_m100_265_stage{1,2,3,4}.md`
- `mydocs/report/task_m100_265_report.md`

### 교훈
범위를 좁게 정하고 시작한 타스크에서 연쇄 발견한 2건의 구조적 버그 (error.rs Debug 누출, main.ts UI 전달 누락) 를 "범위 외" 로 밀어내지 않고 함께 고친 판단이 옳았음. 이를 제외했다면 **"에러 메시지 개선했는데 사용자는 아무 변화 못 느낌"** 결과가 되어 사용자 경험 개선 0 이 되었을 것. Root cause 를 사용자 가시 지점까지 끝까지 추적하는 원칙 재확인.

## 2. Task #280 — 수식 SVG 렌더링 폰트 스택 재정렬

### 배경

작업지시자 제보: `samples/equation-lim.hwp` (`lim _{h→0} {f(2+h)-f(2)} over h`) 가 한컴 PDF 대비 "폰트가 볼드, 크기가 다름". 5단계로 수행.

### 조사 (이 타스크의 실질적 가치)

"크기가 다르다" 는 인상이 PDF 콘텐츠 스트림(`BT /F1 110 Tf ... lim ... BT /F1 92 Tf ... f ...`)에서 `lim=110pt` vs 본문=92pt 로 보여 "함수명 1.2x 확대 규칙" 의심으로 출발. 그러나:

- `exam_math.hwp` 내 16개 수식의 bbox 높이 비교 → `"b= log 2"` = 1125 HWPUNIT (font_size * 1.02) 로 단순 수식과 동일
- **함수명 확대 규칙 없음** 을 데이터로 확정
- PDF 의 110/92 차이는 HyhwpEQ 폰트의 ASCII 글리프(l,i,m) vs PUA 수식 글리프(f,h,(,)) em 박스 차이를 HWP 엔진이 보정한 것

이 판단 오류를 피한 것이 이번 타스크의 핵심 — 1.2x 배율을 잘못 적용했으면 오히려 다른 수식들이 깨졌을 것.

### 변경 범위

- **코드** `src/renderer/equation/svg_render.rs` + `canvas_render.rs` 각 1줄:
  - `EQ_FONT_FAMILY` / `set_font` 에서 **`Cambria Math` 제거** (Windows 볼드 인상의 근본 원인)
  - `'Pretendard'` 제거 (산세리프, 수식 부적합)
  - `'STIX Two Text'`, `'Times New Roman'`, `'Times'` 추가 (클래식 세리프 폴백 체인)
  - 최종 스택: `'Latin Modern Math', 'STIX Two Text', 'STIX Two Math', 'Times New Roman', 'Times', serif`
  - `Latin Modern Math` 첫 번째 유지 → `svg.rs:332` 의 폰트 임베딩 파이프라인 호환
- **샘플** `samples/equation-lim.{hwp,pdf}` 커밋 (재현용)

### 검증

- `cargo test --lib equation`: **48 passed / 0 failed**
- `cargo test --test svg_snapshot`: 3 passed
- `cargo clippy --lib -- -D warnings`: clean
- `cargo check --target wasm32-unknown-unknown --lib`: clean
- 시각 회귀: `equation-lim.hwp` before/after/pdf 비교 + `exam_math.hwp` 5개 페이지 육안 → "볼드 인상" 해소, 레이아웃 변화 없음
- p013 에서 `lim`, `sin`, `ln`, `∫` 가 본문과 동일 크기로 렌더링됨 재확인 (함수명 1.2x 규칙 없음 재증명)

### 산출물

- `mydocs/plans/task_m100_280{,_impl}.md`
- `mydocs/working/task_m100_280_stage{1,2,3,4}.md` + 시각 비교 PNG 다수
- `mydocs/report/task_m100_280_report.md`

### 후속 이슈

- **#283** — 괄호 `(` `)` SVG path 폭이 폰트 글리프 대비 과대 (Phase 2, 별도 이슈 등록 → 오늘 처리 완료)

### 교훈

"PDF 콘텐츠 스트림의 숫자(110 vs 92)" 같은 단일 근거로 스케일 규칙을 추정하지 않고, **샘플 bbox 데이터 다수 교차 비교로 가설 검증** 후 수정 범위 결정. 결과적으로 1줄 수정으로 제보 해결.

## 3. Task #287 — 빈 runs comp_line 의 TAC 수식 인라인 처리 추가

### 배경

작업지시자 제보: `samples/exam_math_8.hwp` 를 SVG 로 출력하면 (가) 박스 안의 큰 cases 수식 `a_{n+1} = { ... }` 이 박스 좌상단(71.80, 147.38)에 찍혀 박스 테두리·선행 텍스트와 겹침. 한컴 PDF(`samples/exam_math_8.pdf`)는 박스 내 별도 줄에 들여쓰기 배치. 4단계로 수행.

### 조사 (이 타스크의 실질적 가치)

초기 가설("`has_tac_shape` 조건 누락 + `.max(y)` clamp")로 수행/구현계획서 작성. 단계 1 실측 덤프 추가해 comp_line 별 y/line_height/runs 확인:

```
[287] para=0 line=1 y=172.69 raw_lh=54.60 line_h=54.60 baseline=32.76 max_fs=0.00
       tac_offsets_px=[(11,9.8,2,Eq), (17,327.6,3,Eq)] runs=[]
```

핵심 발견: **큰 수식이 속한 comp_line(line 1) 은 `runs=[]`(빈 runs)** 이고, `[287-eq] para=0 ci=3` 로그 부재 → 인라인 경로 미진입 확정. TAC 인라인 처리 블록이 run 루프 안에 있어 빈 runs 줄에서 실행 안 됨.

또한 `compose_lines` 가 ls.vpos 를 버리고 `y` 는 순수 누적이라, (A) vpos 파이프라인 전파는 **composer+layout+render_tree+대규모 테스트 재검증** 을 요구하는 조판 엔진 리팩터링 수준. 본 타스크 범위 초과로 판단하고 (C) "빈 runs comp_line 의 TAC 인라인 처리 추가" 로 좁게 해결.

### 변경 범위

- **코드** `src/renderer/layout/paragraph_layout.rs` (+63 줄, run 루프 종료 직후):
  - `comp_line.runs.is_empty() && !tac_offsets_px.is_empty()` 조건에서 이 줄 소유 TAC 수식을 인라인 렌더
  - 줄 char 범위 `[comp_line.char_start, next.char_start)` 로 필터링
  - `y`/`baseline`/`line_height` 는 이 줄 컨텍스트에서 이미 정확 → 그대로 사용
  - `tree.set_inline_shape_position` 등록으로 `shape_layout.rs:133-182` display 경로 중복 렌더 차단
- **샘플** `samples/exam_math_8.{hwp,pdf}` 커밋 (재현용)

### 검증

- `cargo test --lib renderer`: **291 passed / 0 failed**
- `cargo test --lib equation`: **48 passed / 0 failed**
- `cargo test --test svg_snapshot`: **3 passed**
- `cargo clippy --lib -- -D warnings`: clean
- 시각 회귀 (수정 전/후 SVG diff):
  - `exam_math_8.svg`: `(71.80,147.38) → (133.27,188.29)` — **본 타스크 목적 달성**
  - `exam_math_008.svg`: page 8 에도 동일 구조가 자동 개선 (내용 유실 없음)
  - `exam_math_012.svg`: `tree.next_id()` 추가 호출로 ID 1 shift — 시각 영향 없음
  - `equation-lim.svg`: y +0.88 px (display → 인라인 경로 전환 baseline 차이)
  - 나머지 19 페이지: 완전 동일

### 한계 — Phase 2 로 분리

PDF 대비 **x 좌표 약 30-50 px 좌측 치우침**. HWP 문단이 "(가) ... \t <수식>" 처럼 탭으로 들여쓰기 되어있는데 빈 runs 줄에서는 탭 처리가 돌지 않음. `compose_lines` 에서 ls 선행 \t 보존 또는 빈 runs 전용 휴리스틱이 필요. 작업지시자 판단 "B — 현재 상태로 마무리, 별도 이슈" 로 결정.

### 산출물

- `mydocs/plans/task_m100_287{,_impl}.md`
- `mydocs/working/task_m100_287_stage{1,2,3}.md`
- `mydocs/report/task_m100_287_report.md`

### 후속 이슈

- **#288** — 빈 runs 줄의 TAC 수식 x 에 탭 반영 (#287 Phase 2, 별도 이슈 등록)

### 교훈

**"계획 추정 → 실측 → 가설 수정"** 루프의 가치 재확인. 수행·구현계획서에 기록된 가설이 단계 1 덤프로 틀림이 확인되자, 방향을 "조건 확장" 에서 "빈 runs 처리 추가" 로 전환하고 구현계획서 단계 2 섹션을 갱신. 가설에 매달려 잘못된 로직을 작성했다면 동작은 하지만 근본 원인을 놓친 코드가 남았을 것. 또한 (A) vpos 파이프라인 전파의 유혹을 회피하고 범위를 좁힌 판단이 옳았음 — 조판 엔진 리팩터링급 작업을 한 타스크에서 무리하게 떠안았다면 회귀 폭증 + 릴리즈 지연 확실.
## 4. Task #283 — 수식 괄호 path → 글리프 전환 (#280 Phase 2)

### 배경

#280 의 폰트 재정렬 후에도 파렌 `(` `)` 가 얇은 moon 으로 남아 폰트 글리프와 불일치. 박스 할당폭 4.40px 중 실제 곡선은 1.54px (31%), 나머지 69% 가 whitespace → 글자와 gap 벌어져 어색함.

### 조사 (단계 1·2)

Times `(` 실측: advance 4.89 (em 0.333), bbox 5.0, 폭 꽉 참. 현재 rhwp path 는 단일 제어점 quadratic Bezier — 수학적으로 대칭 moon 형상만 생성. **6개 변형 프로토타입(보수/강화 A+C · 글리프 B · 극단 2개) 을 Chrome 렌더로 비교** → path 튜닝 5개 모두 moon 극복 실패, **B_glyph 만 폰트와 일관된 타이포그래피**.

### 선정

**옵션 B (글리프 전환 + 임계치 분기)**:
- `body.height / fs ≤ 1.2` + `(`/`)` → `<text>(</text>` (Times advance 4.89 안에 글리프 자연 배치)
- `> 1.2` → 기존 path 유지 (분수·sum·행렬 감쌈)
- Matrix arm 은 항상 path (변경 없음)

### 변경 범위

- **코드** `src/renderer/equation/layout.rs:832` — `paren_w: fs * 0.3 → fs * 0.333` (Times advance 매치)
- **코드** `src/renderer/equation/svg_render.rs` `LayoutKind::Paren` arm — 높이 + 글리프 종류 분기 (`<text>` / path)
- **코드** `src/renderer/equation/canvas_render.rs` `LayoutKind::Paren` arm — 동일 분기 (`ctx.fill_text` / path)
- **테스트** `test_paren_svg` 갱신 (path → text assertion), `test_paren_stretch_svg` 신규 (스트레치 path 보전 확인)

### 검증

- `cargo test --lib equation`: **49/49** (신규 스트레치 테스트 포함)
- `cargo test --test svg_snapshot`: 3 passed
- `cargo clippy --lib --bins --tests`: 에러 없음
- `cargo check --lib --target wasm32-unknown-unknown`: clean
- `cargo test --lib` 전체: 950 pass / 14 fail (기존 CFB writer, #280 단계 확인 완료)
- 실측: `equation-lim.svg` `<path>` **4건 → 0건**, `<text>(/)</text>` **4건 신규**
- 시각 회귀: AFTER ≈ PDF 레퍼런스 (`compare.png` 3면 비교)
- exam_math.hwp 20페이지 중 4개 샘플링 — 텍스트 높이 글리프 / 스트레치 path 분기 올바름

### 산출물

- `mydocs/plans/task_m100_283{,_impl}.md`
- `mydocs/working/task_m100_283_stage{1,2,3,4}.md` + 단계별 SVG/PNG 산출물
- `mydocs/report/task_m100_283_report.md`

### 후속 과제 후보 (범위 밖)

- 기타 괄호 `{`·`[`·`|` 글리프 전환 (동일 패턴 확장)
- 스트레치 path 품질 개선 (cubic Bezier/다중 세그먼트로 재설계)

### 교훈

1. **path 튜닝 3안을 모두 프로토타입** 해 본 것이 결정적 — "제어점 이동으로 어떻게든 된다" 가설에 갇히지 않고 데이터로 "수학적 한계" 확정. "시각 비교 1회로 글리프 명확히 우세" 로 판단 단순화.
2. **폰트 글리프가 있는데 path 로 재구현하지 말자** — 50년간 다듬어진 Times `(` 를 40줄 Bezier 로 이기려는 싸움은 구조적으로 불리. 도메인 자원(폰트) 을 존중.

## 6. Task #290 — cross-run 탭 감지가 inline_tabs 무시

### 배경

작업지시자 제보: `samples/exam_math.hwp` 페이지 7 의 18번 "수열" 문항 첫 줄이 SVG 출력과 PDF 가 다르게 보임 — `18.` 과 `수열` 사이에 거대한 공백이 생겨 "수열" 이 좌측 단의 우측 끝 근처로 밀려남.

### 원인 (Stage 1)

`src/renderer/layout/paragraph_layout.rs:1213-1226` (render 측) + `:854-868` (est 측) 의 cross-run 우측/가운데 탭 감지 블록이 마지막 `\t` 의 종류를 판정할 때 `find_next_tab_stop` (TabDef 전용) 만 호출. 본문 `tab_extended` (inline_tabs) 는 참조하지 않아, inline 이 LEFT 를 명시한 `\t` 도 TabDef stop 이 모두 abs_before 보다 작으면 `auto_tab_right` 폴스루로 RIGHT (type=1) 오판 → `pending_right_tab_render = Some((420.11, 1))` → 다음 run "수열 이 모든 자연수 " 가 col_area.x + 420.11 - 201.00 = 290.91 px 로 우측 끝 역산 배치.

임시 트레이스 (`RHWP_TRACE290=1`) 로 `ext[2]` 포맷 실증:
- high byte = 탭 종류 enum+1 (1=LEFT, 2=RIGHT, 3=CENTER, 4=DECIMAL)
- low byte = fill_type (TabDef.fill 과 일치)
- exam_math #18 (LEFT × 3): `ext[2]=256=0x0100` → high=1
- hwp-3.0-HWPML 저작권\t1 (RIGHT, fill=3): `ext[2]=515=0x0203` → high=2, low=3

### 변경 범위

- **코드** `src/renderer/layout/paragraph_layout.rs`:
  - 신규 헬퍼 `resolve_last_tab_pending`: inline_tabs 가 마지막 `\t` 커버 시 `ext[2] >> 8` 고바이트로 종류 판정 (LEFT → None, RIGHT/CENTER → TabDef 경로 폴스루)
  - est 측 + render 측 cross-run 블록 2 곳을 헬퍼 호출로 교체
  - `inline_tab_cursor_*: usize` 변수 양쪽 루프에 도입
- **테스트**
  - 단위 5 건 (`src/renderer/layout/tests.rs`): LEFT→None / RIGHT→Some / CENTER→Some / inline 없음 폴백 2 건
  - 통합 1 건 (`tests/tab_cross_run.rs`): exam_math.hwp p.7 "수" glyph x < 200 px

### 검증

- `cargo test --lib task290`: 5/5 pass
- `cargo test --test tab_cross_run`: 1/1 pass
- `cargo test --test svg_snapshot`: 3 passed
- `cargo test --lib` 전체: 955 pass (선존재 cfb_writer 14 fail 무관)
- `cargo clippy --lib`: clean
- **회귀 184 페이지 중 1 페이지만 변경** — git worktree 로 Stage 1 baseline 과 비교:
  - exam_math.hwp 20 페이지: p.7 만 (의도된 item 18 수정, 14 줄 모두 -181.11 px 일관 좌측 이동)
  - biz_plan 6 + exam_eng 11 + exam_kor 25 + hwp-3.0-HWPML 122: 전부 byte-identical
  - **RIGHT inline tab (저작권\t1) 회귀 0**
- 시각 비교 (`mydocs/working/task_m100_290_stage3/p7_{before,after,pdf}.png`) 3 면: AFTER = PDF 일치

### 범위 외 후속 과제

- **inline_tabs RIGHT/CENTER 렌더**: `text_measurement.rs:217, 320` 가 `ext[2]` 전체 u16 을 종류로 해석 → 실제 HWP 값 (최소 256) 과 매칭 안 됨 → inline RIGHT/CENTER 경로 도달 불가. 저작권\t1 은 LEFT 처리로 우연히 우측 정렬과 유사해 보임. 별도 이슈 후보.

### 산출물

- `mydocs/plans/task_m100_290{,_impl}.md`
- `mydocs/working/task_m100_290_stage{1,2,3,4}.md` + 시각 비교 PNG 3 면
- `mydocs/report/task_m100_290_report.md`
- `mydocs/troubleshootings/tab_tac_overlap_142_159.md` 에 #290 섹션 추가 (#142 교훈 확장)

### 교훈

1. #142 의 "**같은 데이터를 다른 경로로 계산하는 코드는 반드시 동기화**" 교훈이 이번엔 `estimate_text_width`/`compute_char_positions` 가 아니라 **run 내부 탭 처리 vs cross-run 탭 감지** 의 불일치로 재발. 런 내부는 inline_tabs 를 봤지만 cross-run 감지는 TabDef 만 봄 → **헬퍼 중앙화** (`resolve_last_tab_pending`) 로 경계 통일.
2. **git worktree baseline diff** 로 184 페이지 byte-level 회귀를 자동 검증. "변경 / 전체 = 1 / 184" 객관적 지표로 fix 가 정확히 의도 범위에만 작용함을 입증.
3. **범위 의식적 제어** — inline_tabs RIGHT/CENTER 렌더 버그를 발견했지만 본 타스크에 포함하지 않고 후속 이슈로 분리. 범위가 커지면 회귀 위험도 증가.

## 7. Task #288 — #287 Phase 2 "not a bug" 결론

### 배경

#287 마무리 시점에 "현재 수식 x=133.27 vs PDF x≈162-182 (약 30-50 px 좌측 치우침)" 라고 **눈대중** 비교로 추정해 Phase 2 (#288) 로 분리 등록. 이슈 원문의 원인 가설은 "HWP composed text 에 `\t` 삽입 + compose 에서 탈락".

### 조사 결과

2 단계 조사로 이슈 전제 모두 부인:

- **단계 1** — `examples/task288_probe.rs` 로 HWP 문단 원본 덤프: `para.text = "(가) 모든 자연수 에 대하여\n\n이다."`. **`\t` 문자 없음**. line 1 의 `runs=[]` 는 `\n\n` 사이에 큰 수식 TAC 이 삽입된 자연스러운 구조.
- **단계 2** — PDF content stream 의 Tm (text matrix) 을 Node.js 로 직접 추출해 수치 비교:

| 요소 | PDF Tm.e | SVG x | 비율 |
|------|----------|-------|------|
| "(" | 481 | 102.07 | 0.2122 |
| 큰 수식 첫 glyph | 627 | 133.27 | **0.2125** |

변환 비 0.2125 로 모든 요소에서 일치. SVG 31.2 px 오프셋 ↔ PDF 환산 31.03 px (0.17 px 오차). **PDF 와 사실상 정확히 일치**.

### 결론

**`not a bug` — #287 수정으로 이미 한컴 PDF 와 일치**. 원래 "162-182" 추정은 PDF 이미지 눈대중에서 나온 오판.

### 변경 범위

- **코드 변경 없음**
- `examples/task288_probe.rs` 임시 진단 파일 작성→제거
- 문서만 커밋: 수행계획서 + 단계 1/2 보고서 + 최종 보고서

### 산출물

- `mydocs/plans/task_m100_288.md`
- `mydocs/working/task_m100_288_stage{1,2}.md`
- `mydocs/report/task_m100_288_report.md`

### 교훈

**눈대중 비교 대신 콘텐츠 스트림 실측**. PDF Tm 직접 추출로 "문제 자체가 없음" 을 판명. 시각 이미지 비교는 스케일·여백 때문에 10-50 px 오차가 흔히 나므로, 정밀 좌표 대조가 필요한 국면에서는 수치 추출이 필수. 또한 부정 확인 직후에는 "다른 규칙이 있을 것" 이라는 가정보다 "증상이 있는가" 를 한 번 더 의심해야 한다. #287 을 (C) 로 좁게 수정한 판단이 결과적으로 **완결적**이었음이 확인됨 — Phase 2 로 분리해 둔 Phase 1 자체가 이미 완성품.
## 4. Task #267 — right tab 3경로 선행 공백 처리 통일

### 배경
KTX.hwp 목차 2페이지에서 소제목 페이지 번호가 ~9.33px 우측으로 밀리는 버그.
right tab 처리 코드가 3경로로 분산되어 있고 탭 직후 선행 공백 처리 방식이 불일치.

### 수정 내용
- `text_measurement.rs` 3곳 — right tab(tab_type==1) 분기에서 `seg_start` 공백 skip
  + `eprintln![DEBUG_TAB_POS]` 잔류 디버그 코드 제거
- `paragraph_layout.rs` 2곳 — `pending_right_tab_est/render` match arm 분리, right tab만 `trim_start()`

### 검증
- `cargo test`: 963 passed, 0 failed
- `cargo clippy --lib -- -D warnings`: 0 warnings
- KTX visual diff: 5/5 PASS, 평균 96.40%

### 산출물
- `mydocs/plans/task_m100_267{,_impl}.md`
- `mydocs/working/task_m100_267_stage{1,2_3}.md`
- `mydocs/report/task_m100_267_report.md`
- `tests/golden_svg/issue-267/ktx-toc-page.svg` + `samples/KTX.hwp`

## 5. Task #275 — WASM canvas 렌더러 OLE RawSvg/Placeholder 처리 복구

### 배경

작업지시자 제보: `samples/bitmap.hwp`, `samples/한셀OLE.hwp` 를 rhwp-studio 에서 열면 파일 로드는 성공하나 본문이 **완전히 빈 페이지**. 네이티브 CLI SVG 내보내기는 정상.

### 처리 방침
3단계 하이퍼워터폴. 이슈→브랜치→수행계획→구현계획→단계별 구현 (Placeholder arm → A 경로 → B 경로 + 검증) → 최종 보고서. 각 단계별 승인 요청.

### 근본 원인

`src/renderer/layout/shape_layout.rs:983-1094` `ShapeObject::Ole` 처리가 OLE 컨테이너 내부에서 네이티브 이미지/EMF/OOXML 차트를 추출해 `RenderNodeType::RawSvg` 로, 추출 실패 시 `RenderNodeType::Placeholder` 로 래핑함. **`svg.rs` 는 두 노드 타입을 처리하지만 `web_canvas.rs` 는 둘 다 match arm 이 없어 `_ =>` 로 빠져 암묵 무시됨** → 빈 페이지.

### 변경 범위
- **코드** `src/renderer/svg_fragment.rs` (신규, +247줄):
  - `find_svg_attr_value`, `try_parse_single_image_data_url`, `decode_base64_data_url`, `is_svg_prefix`, `wrap_svg_fragment` 5 헬퍼
  - 단위 테스트 19건
  - web_canvas (wasm32 전용) 가 아닌 렌더러 공용 모듈로 분리 → 네이티브 테스트 가능
- **코드** `src/renderer/web_canvas.rs`:
  - `RenderNodeType::Placeholder` arm (+26줄) — svg.rs 와 동등 출력 (회색 배경 + 점선 테두리 + 중앙 라벨)
  - `RenderNodeType::RawSvg` arm (+25줄) — A 경로 (`<image data:...>` 단일 요소 직접 디코드) + B 경로 (`<g>...</g>` 복합 SVG 를 `<svg>` 로 래핑 후 draw_image 경유 async 로드)
  - `detect_image_mime_type` 에 SVG 분기 추가 → 별도 `draw_svg` 함수 없이 기존 draw_image 파이프라인 재사용
- **WASM 재빌드**: 4,043,989 → 4,051,019 bytes (+7,030 bytes)

### 검증
- `cargo test --lib svg_fragment`: **19 passed / 0 failed**
- `cargo test --lib`: **968 passed / 14 failed** (+19 신규, baseline 14 실패는 사전 존재, 변화 없음)
- `cargo check --lib --target wasm32-unknown-unknown`: clean
- `wasm-pack build --target web`: 성공
- `npx tsc --noEmit` (rhwp-studio): clean
- E2E (puppeteer + headless Chrome):
  - `bitmap.hwp` 빈 페이지 → **비트맵 손글씨 이미지 정상 렌더**
  - `한셀OLE.hwp` 빈 페이지 → **노란 스프레드시트 이미지 정상 렌더**
  - B 경로 (`<g><rect/><image/><text/></g>` 강제 재현 → 원복): 복합 SVG 전체 렌더 확인
  - Placeholder (FORCE_PLACEHOLDER 가드 → 원복): 점선 테두리 + 중앙 라벨 확인
  - 회귀: `biz_plan.hwp`, `form-002.hwpx` 변화 없음

### 핵심 설계 결정
- **헬퍼 모듈 분리**: `svg_fragment` 를 wasm32 gate 없이 등록 → 네이티브 단위 테스트 확보. web_canvas 는 wasm32-only 이라 내부에 두면 테스트 불가
- **draw_image 재사용**: SVG 전용 `draw_svg_async` 분리 대신 MIME 감지 한 분기로 통합 → IMAGE_CACHE / HtmlImageElement async / 재렌더 로직 중복 방지
- **viewBox = bbox**: 조각 내부의 페이지 절대좌표 = drawImage 위치 (1:1 단순 투영) — 좌표 변환 로직 제거로 디버깅 용이

### 산출물
- `mydocs/plans/task_m100_275{,_impl}.md`
- `mydocs/working/task_m100_275_stage{1,2,3}.md`
- `mydocs/report/task_m100_275_report.md`

### 교훈
- **표면 증상 "빈 페이지"에서 뒤로 3단계** (canvas 렌더러 부재 → RawSvg/Placeholder 노드 타입 → svg.rs/web_canvas.rs 비대칭) 추적이 필요한 버그. 증상만 보면 "파서 버그" 로 오진하기 쉬운데 `rhwp export-svg` (CLI) 가 정상 동작한다는 교차 검증으로 빠르게 렌더 경로로 좁힘
- **두 렌더러 간 match arm 대칭성 검사**가 자동화되어 있지 않음 → 후속: `svg.rs` 와 `web_canvas.rs` 의 `RenderNodeType` match arm 커버리지 비교 테스트 고려 (M101+ 후보)
- **강제 재현 기법**: 프로덕션 샘플이 없는 경로 (B 경로, Placeholder) 도 shape_layout.rs 에 임시 가드를 한 줄 넣어 WASM 재빌드 → 스크린샷 → 원복 사이클로 시각 검증 가능. diff clean 상태로 원복 확인이 핵심

## 7. Task #295 — exam_math.hwp 12쪽 좌단 레이아웃 붕괴 수정

### 배경
12쪽 다단 레이아웃에서 좌측 컬럼 본문(29번 문제)이 페이지 하단으로 밀려 압축·겹침. LAYOUT_OVERFLOW 18건.

### 근본 원인
`layout_table_item::renders_above_body` 분기가 vert=Paper + 본문 위 케이스만 out-of-flow 처리. pi=22 푸터 표(`vert=Page valign=Bottom`, 본문 아래)가 in-flow로 처리되어 `y_offset`이 푸터 위치(≈y=1371)로 점프 → 후속 좌단 본문 모두 본문 영역 하단/이하 배치.

### 변경 범위 (`src/renderer/layout.rs`)
- `renders_outside_body`(≈1995): vert Paper → Paper|Page; 위치 `tbl_y < body.y || tbl_y + tbl_h > body_bottom`
- `is_outside_body`(≈2103): 동일 확장 (spacing 가드)
- `tbl_inline_x` Square 분기(≈1978): halign 반영 (Left/Right/Center/Outside)
- 어울림 호출 가드 제거(≈2086): `wrap_around_paras` 비어 있어도 호출 → 자가 wrap host 본문 복원
- `layout_wrap_around_paras` 호스트 텍스트(≈2509): 첫 줄만 → 전체 줄 렌더링

### 검증
- LAYOUT_OVERFLOW: 18건 → **0건** (12쪽 + 전체 샘플)
- pi=23 y: 1340.1 ❌ → 178.7 ✅
- pi=27 표 머리행/호스트 본문 5줄/halign=Right 모두 복원
- `cargo test --release`: **1028 passed / 0 failed**
- 회귀: `exam_math.hwp` 1쪽(머리말 vert=Page 표) 정상 유지, `exam_math_no.hwp`/`equation-lim.hwp`/`text-align-2.hwp` LAYOUT_OVERFLOW 0건

### 산출물
- `mydocs/plans/task_m100_295{,_impl}.md`
- `mydocs/working/task_m100_295_stage{1,3,4}.md`
- `mydocs/report/task_m100_295_report.md`

### 후속 이슈
- **#297** — 12쪽 우측 컬럼 단락 높이 과대로 동전 그림 위치 어긋남. #295 수정 전·후 좌표 동일(147.4..497.3)인 사전 존재 버그로 별도 분리. 줄높이/line_spacing/인라인 수식(tac=true) 메트릭 의심 → **오늘 #297로 해결** (실제 원인은 `VertRelTo::Page` vs `Paper` 기준점 미구분이었음. "단락 높이 과대" 증상은 오인이고 실제는 `vert=Page` 푸터 표의 ref 영역 오산으로 박스가 본문 경계 밖으로 147px 밀려난 것)
- 머리말 페이지번호 4↔2 불일치 (1단계 보고서에서 분리 권고)

### 교훈
- 1단계에서 `vert=Paper`로 추정했으나 임시 `eprintln!` 디버그로 실제 `vert=Page` 확인 → 가설 정정. **추정만으로 수정 범위 확정하지 말고 실측 데이터로 가설 재확인**하는 절차 가치 재증명
- 좌단 붕괴 수정 후 드러난 잔여 3문제(표 머리행/호스트 본문/halign)를 동일 타스크에서 일괄 처리. 사용자 가시 지점까지 끝까지 추적 원칙 적용
- 우측 컬럼 줄높이 문제는 본 타스크와 무관(좌표 무변화)임을 좌표 비교로 확정 후 #297로 분리 — **범위 확장 유혹을 데이터로 차단**

## 8. Task #296 — WASM Canvas 경로 inline_tabs 존중 (#290 Canvas 버전, 메인테이너 핀셋)

### 배경

PR #292 (#290) 머지 후 브라우저 검증에서 `exam_math.hwp` p.7 #18 "수열" 문항이 여전히 우측으로 밀림:
- SVG (CLI): ✅ 정상 (`translate(109.80, ...)`)
- Canvas (브라우저): ❌ x≈290.91

네이티브 SVG 경로 수정 (PR #292 의 paragraph_layout.rs cross-run 감지) 이 Canvas 측 text_measurement.rs 에 반영되지 않음. 메인테이너가 핀셋 처리로 직접 해결.

### 근본 원인

`src/renderer/layout/text_measurement.rs` 두 측정기 비대칭:
- `EmbeddedTextMeasurer` (네이티브): inline_tabs 분기 존재 (단, 버그 보유 — `tab_type = ext[2]` 전체 u16 해석)
- `WasmTextMeasurer` (WASM): **inline_tabs 분기 자체 부재** → TabDef 만 참조 → `auto_tab_right` 폴스루 → RIGHT 오판

### 변경 범위 (축소판)

- **코드** `src/renderer/layout/text_measurement.rs` (+69 -2):
  - 헬퍼 `inline_tab_type(ext)` = `(ext[2] >> 8) & 0xFF` 신규 (pub(super))
  - `WasmTextMeasurer::estimate_text_width` 에 inline_tabs 분기 신규 (+23)
  - `WasmTextMeasurer::compute_char_positions` 에 동일 분기 신규 (+27)
  - `EmbeddedTextMeasurer` 는 **건드리지 않음** — 기존 golden 2건 (issue-147, issue-267) 이 "우연한 LEFT 폴백"에 의존 중이라 범위 축소
- **테스트** `src/renderer/layout/tests.rs` (+32): `task296_inline_tab_type_{left,right,center,decimal}` 4건

### 범위 축소 결정

Stage 2 중간에 네이티브 수정을 시도했으나 svg_snapshot 2건 (issue-147 "(페이지표기)", issue-267 KTX 목차) FAIL. 한컴 PDF 대조 없이 골든 갱신은 위험하다고 판단, 범위를 WASM 경로만 수정으로 축소. 네이티브 측 일관성 복원은 후속 이슈 후보로 기록.

### 검증

- `cargo test --lib`: 992 passed / 0 failed (988 → 992, +4 신규 task296)
- `cargo test --test svg_snapshot`: 6 passed (기존 골든 유지)
- `cargo test --test tab_cross_run`: 1 passed (#290 회귀 없음)
- `cargo clippy` / `cargo check --target wasm32`: clean
- WASM Docker 빌드: 성공 (`pkg/rhwp_bg.wasm` +3,918 bytes)
- **rhwp-studio 브라우저 시각 검증**: ✅ 작업지시자 판정 성공

### 산출물

- `mydocs/plans/task_m100_296{,_impl}.md`
- `mydocs/working/task_m100_296_stage{1,2,3}.md`
- `mydocs/report/task_m100_296_report.md`
- `mydocs/troubleshootings/tab_tac_overlap_142_159.md` — "#296 섹션" 추가

### 교훈

1. **범위 축소 결정의 가치** — 기존 골든이 깨지는 것을 Stage 2 중에 확인한 시점에 방향 전환. 무리하게 밀어붙여 골든을 수정했다면 한컴 PDF 대조 없이 잘못된 방향으로 갱신할 위험. 두 측정기의 동작 차이를 허용하고 `pub(super)` 헬퍼만 남겨 후속 작업의 공유 자원으로 활용.
2. **누적 교훈 (#142 → #290 → #296)** — "같은 데이터를 다른 경로로 계산하는 코드는 헬퍼로 중앙화". 이번에도 `inline_tab_type` 을 중앙화하여 후속 이슈에서 네이티브 측정기가 동일 헬퍼를 재사용 가능.
## 7. Task #297 — exam_math.hwp 12쪽 "* 확인 사항" 박스 위치 오류 수정

### 배경

#295 마무리 시점에 "12쪽 우측 컬럼 단락 높이 과대 → 동전 그림 위치 어긋남"으로 분리 등록한 #297. 초기 가설은 pi=28/30/32 문단이 매 줄 `line_spacing`을 가산해 PDF 대비 과대라는 "줄높이" 문제.

### 조사 결과 — 증상 오인

실측 좌표 비교(`pdftotext -bbox-layout`):
- **동전 이미지(pi=33)**: PDF y≈495, SVG y=497.25 → **일치**
- **"\* 확인 사항" 박스**: PDF y≈1231, SVG y=1373.7 → **+142 px 드리프트**

이슈 제목의 "동전 위치"는 오인. 실제는 본문 pi=22의 `VertRelTo::Page` 푸터 표(size=419.5×134.7 px)가 밀려난 것.

### 원인 정정 (v1 가설 폐기 후 재조사)

1단계 가설: 바탕쪽(master page) `vert=Paper/101954 HU` 표의 bottom-anchoring 필요 → 적용해 봤으나 시각 변화 없음(해당 표는 빈 1x3이었음).

재조사 결과: 실제 "\* 확인 사항"은 본문 pi=22의 `vert=Page/141, valign=Bottom` 표. `compute_table_y_position`이 `VertRelTo::Page`와 `Paper`를 동일하게 `(ref_y=0, ref_h=page_h_approx)`로 처리 → Page 기준이 용지 전체가 되어 버려 푸터 표가 147 px 아래로 밀림.

### 변경 범위

- **코드** `src/renderer/layout/table_layout.rs:986-992` (1줄 변경):
  - `VertRelTo::Page => (0.0, page_h_approx)` → `(col_area.y, col_area.height)` (본문 영역 기준)
  - `VertRelTo::Paper`는 그대로 (용지 전체)
  - 주석 3줄 추가 (Task #297 참조)

### 수치 검증

pi=22 표 (v_offset=141 HU=1.9px, valign=Bottom, table_h=134.7):
- 수정 전: `0 + 1508.1 − 134.7 − 1.9 = 1371.5 px`
- 수정 후: `147.4 + 1213.3 − 134.7 − 1.9 = 1224.1 px` (PDF 1226.5 ± 2 px 일치)

### 검증

- `cargo test --release --lib`: **988 passed / 0 failed**
- `cargo clippy`: 경고 없음
- 회귀 스캔 (145 샘플 중 본문 `VertRelTo::Page` 표 13건):
  - exam_math.hwp / exam_math_no.hwp 12·16·20쪽: 의도된 수정 반영 ✓
  - tac-case-001..005: diff=0 (무회귀) ✓
  - exam_social / exam_eng: -1 byte 수치 포맷 미차
  - 바탕쪽 `VertRelTo::Page` 5건: 모두 diff=0 (col_area=paper_area 조건 수학적 동치) ✓
- exam_math.hwp 20페이지 중 12·16·20만 변경, 나머지 17페이지 byte-identical ✓
- 시각 비교(PDF vs SVG p12/p16 병렬): 박스 위치 일치 확인

### 산출물

- `mydocs/plans/task_m100_297{,_impl}.md` (impl은 v2로 교체)
- `mydocs/working/task_m100_297_stage{1,3}.md`
- `mydocs/report/task_m100_297_report.md`

### 교훈

1. **가설은 가장 빠른 검증으로 조기 반증** — 1단계에서 바탕쪽 `vert=Paper` 가설을 가지고 수행·구현계획서까지 작성했으나 3단계 진입 후 실제 수정을 붙이자 시각 변화 없음이 즉시 드러남. 이 순간 가설 폐기하고 본문 pi=22로 범위 재정의한 판단이 옳았음. 실측 우선.
2. **이슈 제목의 증상 기술을 맹신하지 말 것** — "동전 그림 위치 어긋남"은 오인이었고 실제는 푸터 박스. PDF text 실제 bbox 추출(`pdftotext -bbox-layout`)로 수치 비교하자 증상의 대상이 완전히 다른 요소임이 판명.
3. **근본 원인의 단순함** — 147 px 드리프트가 결국 1줄 수정(Page 기준점 분리)으로 해결. Page/Paper enum 구분을 스펙대로 번역하지 않은 오래된 코드 부채였음.

## 6. Task #295 — exam_math.hwp 12쪽 좌단 레이아웃 붕괴 수정

### 배경
12쪽 다단 레이아웃에서 좌측 컬럼 본문(29번 문제)이 페이지 하단으로 밀려 압축·겹침. LAYOUT_OVERFLOW 18건.

### 근본 원인
`layout_table_item::renders_above_body` 분기가 vert=Paper + 본문 위 케이스만 out-of-flow 처리. pi=22 푸터 표(`vert=Page valign=Bottom`, 본문 아래)가 in-flow로 처리되어 `y_offset`이 푸터 위치(≈y=1371)로 점프 → 후속 좌단 본문 모두 본문 영역 하단/이하 배치.

### 변경 범위 (`src/renderer/layout.rs`)
- `renders_outside_body`(≈1995): vert Paper → Paper|Page; 위치 `tbl_y < body.y || tbl_y + tbl_h > body_bottom`
- `is_outside_body`(≈2103): 동일 확장 (spacing 가드)
- `tbl_inline_x` Square 분기(≈1978): halign 반영 (Left/Right/Center/Outside)
- 어울림 호출 가드 제거(≈2086): `wrap_around_paras` 비어 있어도 호출 → 자가 wrap host 본문 복원
- `layout_wrap_around_paras` 호스트 텍스트(≈2509): 첫 줄만 → 전체 줄 렌더링

### 검증
- LAYOUT_OVERFLOW: 18건 → **0건** (12쪽 + 전체 샘플)
- pi=23 y: 1340.1 ❌ → 178.7 ✅
- pi=27 표 머리행/호스트 본문 5줄/halign=Right 모두 복원
- `cargo test --release`: **1028 passed / 0 failed**
- 회귀: `exam_math.hwp` 1쪽(머리말 vert=Page 표) 정상 유지, `exam_math_no.hwp`/`equation-lim.hwp`/`text-align-2.hwp` LAYOUT_OVERFLOW 0건

### 산출물
- `mydocs/plans/task_m100_295{,_impl}.md`
- `mydocs/working/task_m100_295_stage{1,3,4}.md`
- `mydocs/report/task_m100_295_report.md`

### 후속 이슈
- **#297** — 12쪽 우측 컬럼 단락 높이 과대로 동전 그림 위치 어긋남. #295 수정 전·후 좌표 동일(147.4..497.3)인 사전 존재 버그로 별도 분리. 줄높이/line_spacing/인라인 수식(tac=true) 메트릭 의심 → **오늘 #297로 해결** (실제 원인은 `VertRelTo::Page` vs `Paper` 기준점 미구분이었음. "단락 높이 과대" 증상은 오인이고 실제는 `vert=Page` 푸터 표의 ref 영역 오산으로 박스가 본문 경계 밖으로 147px 밀려난 것)
- 머리말 페이지번호 4↔2 불일치 (1단계 보고서에서 분리 권고)

### 교훈
- 1단계에서 `vert=Paper`로 추정했으나 임시 `eprintln!` 디버그로 실제 `vert=Page` 확인 → 가설 정정. **추정만으로 수정 범위 확정하지 말고 실측 데이터로 가설 재확인**하는 절차 가치 재증명
- 좌단 붕괴 수정 후 드러난 잔여 3문제(표 머리행/호스트 본문/halign)를 동일 타스크에서 일괄 처리. 사용자 가시 지점까지 끝까지 추적 원칙 적용
- 우측 컬럼 줄높이 문제는 본 타스크와 무관(좌표 무변화)임을 좌표 비교로 확정 후 #297로 분리 — **범위 확장 유혹을 데이터로 차단**

## 9. Task #301 — exam_math.hwp 12쪽 z-table 셀 수식 이중 렌더링 수정

### 배경

작업지시자 제보: `samples/exam_math.hwp` p12 좌측 #29 안의 정규분포 z-table에서 모든 셀의 숫자/헤더가 SVG 출력 시 두 번 그려져 텍스트가 겹침. 한컴 PDF는 1회 렌더로 정상.

### 근본 원인 — Task #287 회귀

z-table 셀은 `text=""` + `Equation` 컨트롤 1개 구조. 빈 runs를 가진 셀 paragraph의 TAC 수식이 두 경로에서 EquationNode로 emit:
1. `paragraph_layout.rs:1996-2057` (Task #287 신규) — 빈-runs 인라인 EquationNode + `set_inline_shape_position` 호출
2. `table_layout.rs:1602-1648` (기존) — `has_text_in_para=false` 분기에서 직접 emit, 중복 검사 없음

→ Task #287 도입 시 table_layout 측 가드를 함께 추가하지 않은 것이 원인.

### 변경 범위

- **코드** `src/renderer/layout/table_layout.rs:1602-1620` (+5 -1):
  - `tree.get_inline_shape_position(section_index, cp_idx, ctrl_idx).is_some()` 가드 추가
  - 이미 렌더된 경우 `inline_x`만 진행, 직접 emit 스킵
- **테스트** `tests/issue_301.rs` (신규, +45):
  - z-table 값(`0.1915`/`0.3413`/`0.4332`) 각 1회 출현 + `0.4772` 2회(z-table + 본문) 검증

### 검증

- `cargo test --release`: **1039 passed / 0 failed / 1 ignored**
- `cargo clippy --release`: clean
- 기존 SVG 골든 6건 무회귀 → 영향 범위가 빈 텍스트 셀 + TAC 수식 케이스에 한정됨을 확증
- 회귀 스캔 (exam_math.hwp 20쪽 + exam_math_no.hwp 20쪽 + exam_math_8.hwp 1쪽): 셀 내 중복 패턴 0건
- 시각 검증 (inkscape PNG): z-table 텍스트 겹침 사라짐

### 산출물

- `mydocs/plans/task_301{,_impl}.md`
- `mydocs/working/task_301_stage{1,2,3}.md`
- `mydocs/report/task_301_report.md`

### 교훈

**양방향 렌더 경로의 동기화 검증**. Task #287에서 paragraph_layout에 빈-runs TAC 수식 처리를 추가할 때 table_layout의 기존 fallback과의 중복 가능성이 점검되지 않아 회귀가 발생. 같은 노드를 emit할 수 있는 두 경로가 있는 구조에서는 한쪽 추가 시 다른쪽 가드를 함께 점검하는 절차가 필요. 본 수정은 `set_inline_shape_position`이라는 기존 메커니즘을 그대로 `is_rendered` 플래그로 재활용하여 별도 상태 추가 없이 해결.

## 9. Task #297 — exam_math.hwp 12쪽 "* 확인 사항" 박스 위치 오류 수정

### 배경

#295 마무리 시점에 "12쪽 우측 컬럼 단락 높이 과대 → 동전 그림 위치 어긋남"으로 분리 등록한 #297. 초기 가설은 pi=28/30/32 문단이 매 줄 `line_spacing`을 가산해 PDF 대비 과대라는 "줄높이" 문제.

### 조사 결과 — 증상 오인

실측 좌표 비교(`pdftotext -bbox-layout`):
- **동전 이미지(pi=33)**: PDF y≈495, SVG y=497.25 → **일치**
- **"\* 확인 사항" 박스**: PDF y≈1231, SVG y=1373.7 → **+142 px 드리프트**

이슈 제목의 "동전 위치"는 오인. 실제는 본문 pi=22의 `VertRelTo::Page` 푸터 표(size=419.5×134.7 px)가 밀려난 것.

### 원인 정정 (v1 가설 폐기 후 재조사)

1단계 가설: 바탕쪽(master page) `vert=Paper/101954 HU` 표의 bottom-anchoring 필요 → 적용해 봤으나 시각 변화 없음(해당 표는 빈 1x3이었음).

재조사 결과: 실제 "\* 확인 사항"은 본문 pi=22의 `vert=Page/141, valign=Bottom` 표. `compute_table_y_position`이 `VertRelTo::Page`와 `Paper`를 동일하게 `(ref_y=0, ref_h=page_h_approx)`로 처리 → Page 기준이 용지 전체가 되어 버려 푸터 표가 147 px 아래로 밀림.

### 변경 범위

- **코드** `src/renderer/layout/table_layout.rs:986-992` (1줄 변경):
  - `VertRelTo::Page => (0.0, page_h_approx)` → `(col_area.y, col_area.height)` (본문 영역 기준)
  - `VertRelTo::Paper`는 그대로 (용지 전체)
  - 주석 3줄 추가 (Task #297 참조)

### 수치 검증

pi=22 표 (v_offset=141 HU=1.9px, valign=Bottom, table_h=134.7):
- 수정 전: `0 + 1508.1 − 134.7 − 1.9 = 1371.5 px`
- 수정 후: `147.4 + 1213.3 − 134.7 − 1.9 = 1224.1 px` (PDF 1226.5 ± 2 px 일치)

### 검증

- `cargo test --release --lib`: **988 passed / 0 failed**
- `cargo clippy`: 경고 없음
- 회귀 스캔 (145 샘플 중 본문 `VertRelTo::Page` 표 13건):
  - exam_math.hwp / exam_math_no.hwp 12·16·20쪽: 의도된 수정 반영 ✓
  - tac-case-001..005: diff=0 (무회귀) ✓
  - exam_social / exam_eng: -1 byte 수치 포맷 미차
  - 바탕쪽 `VertRelTo::Page` 5건: 모두 diff=0 (col_area=paper_area 조건 수학적 동치) ✓
- exam_math.hwp 20페이지 중 12·16·20만 변경, 나머지 17페이지 byte-identical ✓
- 시각 비교(PDF vs SVG p12/p16 병렬): 박스 위치 일치 확인

### 산출물

- `mydocs/plans/task_m100_297{,_impl}.md` (impl은 v2로 교체)
- `mydocs/working/task_m100_297_stage{1,3}.md`
- `mydocs/report/task_m100_297_report.md`

### 교훈

1. **가설은 가장 빠른 검증으로 조기 반증** — 1단계에서 바탕쪽 `vert=Paper` 가설을 가지고 수행·구현계획서까지 작성했으나 3단계 진입 후 실제 수정을 붙이자 시각 변화 없음이 즉시 드러남. 이 순간 가설 폐기하고 본문 pi=22로 범위 재정의한 판단이 옳았음. 실측 우선.
2. **이슈 제목의 증상 기술을 맹신하지 말 것** — "동전 그림 위치 어긋남"은 오인이었고 실제는 푸터 박스. PDF text 실제 bbox 추출(`pdftotext -bbox-layout`)로 수치 비교하자 증상의 대상이 완전히 다른 요소임이 판명.
3. **근본 원인의 단순함** — 147 px 드리프트가 결국 1줄 수정(Page 기준점 분리)으로 해결. Page/Paper enum 구분을 스펙대로 번역하지 않은 오래된 코드 부채였음.

## 이슈 활동

### 종료
- [#301](https://github.com/edwardkim/rhwp/issues/301) — exam_math.hwp p12 z-table 셀 수식 이중 렌더링 수정 (Task #287 회귀, table_layout Equation 분기에 `get_inline_shape_position` 가드 추가. 5줄 수정 + 회귀 테스트 1건. 1039 passed / 0 failed.)
- [#297](https://github.com/edwardkim/rhwp/issues/297) — exam_math.hwp 12쪽 "* 확인 사항" 박스 위치 오류 수정 (`VertRelTo::Page` vs `Paper` 기준점 미구분이 근본 원인. Page → body_area, Paper → paper 전체로 분리. 1줄 수정. 회귀 0건.)
- [#265](https://github.com/edwardkim/rhwp/issues/265) — HWP 3.0 파일 감지 + 친절한 에러 메시지로 해결
- [#280](https://github.com/edwardkim/rhwp/issues/280) — 수식 SVG 폰트 스택 재정렬 (Cambria Math 제거 → Times New Roman 폴백)
- [#283](https://github.com/edwardkim/rhwp/issues/283) — 수식 괄호 path → 글리프 전환 (#280 Phase 2, PR #285 로 devel merge)
- [#287](https://github.com/edwardkim/rhwp/issues/287) — 빈 runs comp_line 의 TAC 수식 인라인 처리 추가 (exam_math_8 박스 좌상단 겹침 해결)
- [#288](https://github.com/edwardkim/rhwp/issues/288) — `not a bug` close (#287 수정으로 이미 PDF 와 일치함을 실측 확인)
- [#275](https://github.com/edwardkim/rhwp/issues/275) — WASM canvas RawSvg/Placeholder 처리 복구 (OLE 렌더 복구)
- [#290](https://github.com/edwardkim/rhwp/issues/290) — cross-run 탭 감지 inline_tabs 존중 (SVG 경로, PR #292)
- [#296](https://github.com/edwardkim/rhwp/issues/296) — WASM Canvas 경로 inline_tabs 무시 (Task #290 의 Canvas 버전, 메인테이너 핀셋 처리)
- [#295](https://github.com/edwardkim/rhwp/issues/295) — exam_math.hwp 12쪽 좌단 레이아웃 붕괴 수정 (vert=Page 푸터 표 out-of-flow + Square wrap 호스트 본문 복원)
- [#297](https://github.com/edwardkim/rhwp/issues/297) — exam_math.hwp 12쪽 "* 확인 사항" 박스 위치 오류 수정 (VertRelTo::Page vs Paper 기준점 미구분이 근본 원인. Page → body_area, Paper → paper 전체로 분리. 1줄 수정. 회귀 0건.)

### 등록·대기
- (없음 — 오늘 사이클 내 모든 이슈 종료)
- [#283](https://github.com/edwardkim/rhwp/issues/283) — 수식 괄호 path → 글리프 전환 (#280 Phase 2)
- [#290](https://github.com/edwardkim/rhwp/issues/290) — cross-run 탭 감지가 inline_tabs 무시하여 LEFT inline 탭이 `auto_tab_right` 폴스루로 RIGHT 오판 → `resolve_last_tab_pending` 헬퍼로 보정. exam_math.hwp p.7 #18 "수열" 문항 수정 ("수" glyph x 290.9 → 109.8). 회귀 184 페이지 중 1 페이지 (의도 100%). 단위 5 + 통합 1 테스트 추가.
- [#275](https://github.com/edwardkim/rhwp/issues/275) — WASM canvas RawSvg/Placeholder 처리 복구 (OLE 렌더 복구)
- [#295](https://github.com/edwardkim/rhwp/issues/295) — exam_math.hwp 12쪽 좌단 레이아웃 붕괴 수정 (vert=Page 푸터 표 out-of-flow + Square wrap 호스트 본문 복원)

### 신규 등록
- (없음)

### PR 제출·대기
- [PR #273](https://github.com/edwardkim/rhwp/pull/273) — Task #267 right tab 정렬 수정, 머지 대기

## 외부 기여자·제보자

- [@jangster77](https://github.com/jangster77) — 첫 이슈 제보 (#265). 3개 경로 (HOP · Chrome 확장 · github.io) 모두에서 재현 확인한 구체적 제보 + 스크린샷 + 구글 드라이브 첨부 파일로 빠른 진단 가능했음. 감사.

## 감사

- 어제 사이클 후반부 (23일 저녁~밤) 이후 오늘 아침까지 이어진 피드백:
  - PR #262 (@planet6897) 1분 응답 대응의 여운 — 기여자 품질이 작업 생산성 자체를 끌어올림
  - PR #264 (@planet6897) assign 누락 사건 교훈 → **오늘 #265 에 즉시 assign 실행** 으로 재발 방지 실천
