# 오늘 할일 - 2026년 4월 25일

## 요약

작업지시자 제보로 `samples/21_언어_기출_편집가능본.hwp` 의 SVG 출력이 1쪽부터 PDF 원본과 불일치함을 확인. 증상은 (A) 바탕쪽 글상자 중복 렌더, (B) 우측 단 본문 누락 두 건처럼 보였으나, 덤프 분석 결과 단일 원인(`SectionDef.flags`의 `hide_master_page` 비트 오프셋 오류)으로 드러남. Task #304 로 등록 후 빠른 경로로 2줄 수정 · 1038 테스트 통과 · 4개 샘플 시각 회귀 확인 완료.

## Task #304 — 21_언어 1쪽 바탕쪽 중복 + 우측단 누락

### 배경
- 제보: `samples/21_언어_기출_편집가능본.hwp` + PDF 비교 결과 1쪽부터 어긋남
- 2쪽 이후는 정상 → 1쪽 전용 문제

### 원인
- HWP5 스펙상 구역정의 속성 bit 2가 "바탕쪽 감춤(첫쪽)" 플래그인데, 기존 파서는 bit 10으로 읽어 오판정
- 이로 인해 1쪽에도 바탕쪽 글상자가 그려져 body 표 header(4×5 표 안의 제목·수험번호 폼)와 중복
- 우측 단 누락은 바탕쪽 오버레이가 덮은 파생 증상이었음 (별도 수정 불필요)

### 변경 범위
- **코드** (2개 파일 2줄):
  - `src/parser/body_text.rs:549` — 읽기 경로 bit 10 → bit 2
  - `src/document_core/queries/rendering.rs:166` — 쓰기 경로 bit 10 → bit 2
- **문서**: 수행계획서 · 구현계획서 · 단계1 보고 · 최종 보고서

### 검증
- `cargo test --release`: 1038 passed, 0 failed
- `cargo clippy --release -- -D warnings`: 경고 없음
- 시각 회귀:
  - 21_언어 1쪽 PDF 일치 ✓ · 2쪽+ 정상 ✓
  - exam_kor / exam_eng (동일 flags 패턴): 잠재 중복 해소 ✓
  - exam_math (flags 비설정): 변화 없음 ✓

### 상태
✅ 완료 (구현 + 보고서 + 검증 통과). merge 대기.

## 향후 과제 (별도 이슈 권장)

`SectionDef.flags` 나머지 hide 비트(header/footer/border/fill/page_num)도 HWP5 스펙(bit 0,1,3,4,5)과 현 코드(bit 8,9,11,12) 오프셋이 다를 가능성. 현재 증상 없음이나 전수 대조 필요.

## Task #306 — 21_언어 페이지네이션 과잉 (분석만 완료)

### 배경
Task #304 완료 후 SVG가 19쪽(PDF 15쪽) 발생을 별개 이슈로 등록.

### 조사 결론
`dump -s 0 -p 50` 에서 LINE_SEG `vpos=0` 리셋 패턴 발견. HWP 파일은 각 줄의 열/쪽 경계를 LINE_SEG vpos로 사전 계산해 저장하나, 우리 엔진은 이 정보 무시하고 파라그래프 높이 자체 계산으로 배치 → 21_언어처럼 LINE_SEG-엔진 간 오차 누적되는 샘플에서 쪽수 초과.

시도한 휴리스틱(Column break 비활성화/조건부)은 exam_math 회귀(20→10쪽) 유발로 모두 기각. 구조적 재설계 필요.

### 변경 범위
- **코드**: 없음 (실험 모두 원복)
- **문서**:
  - `mydocs/plans/task_m100_306.md` 수행계획서
  - `mydocs/working/task_m100_306_analysis.md` 분석 보고서

### 상태
🔎 조사만 완료. 이슈 #306 OPEN 유지. 본질적 수정은 페이지네이션 엔진 재설계(Epic 수준) 로 분리 권장.

## Epic #309 — 페이지네이션 엔진 LINE_SEG vpos 우선 모드 전환

Task #306 분석 결과 본질 수정이 단일 타스크 범위를 벗어나므로 Epic 이슈로 등록하고 단계별 sub-issue로 분리.

## Task #310 — 1단계: LINE_SEG vpos 노출 + 검증 도구 추가

### 변경 범위
- **코드 (3 파일)**:
  - `src/document_core/queries/rendering.rs` — dump-pages 출력에 vpos + `[vpos-reset@line{n}]` 마커
  - `src/renderer/render_tree.rs` — TextLineNode에 line_index/vpos 필드 추가
  - `src/renderer/layout/paragraph_layout.rs` — 본문 메인 루프에서 with_para_vpos 사용
  - `src/renderer/svg.rs` — --debug-overlay 에 vpos-reset 가로 점선 마커
- **문서**: 수행계획서 · 구현계획서 · 단계1·2·3 보고서 · 최종 보고서 · `mydocs/tech/line_seg_vpos_analysis.md`

### 핵심 발견
| 샘플 | SVG 쪽 | vpos-reset | FullParagraph 내부 reset |
|------|--------|-----------|--------------------------|
| 21_언어 | 19 (+4) | 13 | **7** ← 어긋남 원인 |
| exam_math | 20 ✓ | 0 | 0 |
| exam_kor | 25 | 8 | 0 |
| exam_eng | 11 ✓ | 0 | 0 |

21_언어 +4쪽 과잉의 원인이 "FullParagraph 내부 vpos-reset 7건" 으로 특정됨. 다른 3개 샘플은 이 패턴 0건 → **2단계 작업 범위가 전면 재설계에서 단일 조건 분기로 축소**.

### 검증
- `cargo test`: 992 passed, 0 failed
- 옵트인: --debug-overlay 미사용 시 SVG 출력 무변동
- 4개 샘플 페이지 수 변화 0

### 상태
✅ 완료. Epic #309 의 다음 sub-issue 후보 명확화 (FullParagraph 내부 vpos-reset 시 PartialParagraph 분리 강제).

## Task #311 — vpos-reset 강제 분리 (실험, 가설 부정)

### 변경 범위
- **코드**:
  - `src/renderer/pagination.rs` — PaginationOpts 구조체 (hide_empty_line + respect_vpos_reset)
  - `src/renderer/pagination/engine.rs` — paginate_with_forced_breaks 신설, paginate_text_lines 옵션 인자
  - `src/document_core/mod.rs` `commands/document.rs` `queries/rendering.rs` — DocumentCore.respect_vpos_reset 필드 + 옵션 전달
  - `src/wasm_api.rs` — set_respect_vpos_reset 셋터
  - `src/main.rs` — --respect-vpos-reset CLI 플래그 (export-svg/dump-pages)
- **문서**: 수행계획서·구현계획서·stage1-3 보고서·최종 보고서·tech 분석 보고서 부록 A

### 핵심 발견 (가설 부정)
| 샘플 | OFF | ON | 평가 |
|------|-----|----|----|
| 21_언어 | 19쪽 | **20쪽 (+1)** | ❌ 악화 |
| exam_math | 20쪽 ✓ | 20쪽 ✓ | 무변화 |
| exam_kor | 25쪽 | 25쪽 | 무변화 |
| exam_eng | 11쪽 ✓ | 11쪽 ✓ | 무변화 |

vpos-reset 강제 분리만으로는 페이지 수 감소 효과 없음. 진짜 원인: 우리 엔진 column 가용 공간 계산이 HWP보다 약 30px 관대 → 분리해도 다른 paragraphs가 다음 단으로 밀려 누적 overflow.

### 검증
- `cargo test`: 992 passed, 0 failed
- 옵션 OFF (기본): 4샘플 페이지 수 무변화 (회귀 0)

### 상태
🔍 가설 부정. 실험 플래그(`--respect-vpos-reset`) + 인프라(`paginate_with_forced_breaks`)는 코드베이스에 보존. 다음 sub-issue 후보: "column 가용 공간 계산 정확도 조사" (Epic #309).

## Task #312 — column 가용 공간 정확도 조사 (의외의 발견)

### 변경 범위
- **코드**:
  - `src/renderer/pagination.rs` `state.rs` `typeset.rs` — ColumnContent.used_height 필드 + flush_column 저장
  - `src/document_core/queries/rendering.rs` — dump-pages 단 헤더에 used/hwp_used/diff 출력
  - `src/renderer/layout/tests.rs` — fixture 5건에 used_height 추가
- **문서**: 수행계획서·구현계획서·stage1-3 보고서·최종 보고서

### 핵심 발견 (의외)
당초 가설(단일 column 정확도 origin 식별/보정)은 부정. 대신 코드베이스에 이미 존재하는 `TypesetEngine` 이 4샘플 모두에서 Paginator 보다 정확:

| 샘플 | Paginator | TypesetEngine | PDF | 평가 |
|------|-----------|---------------|-----|------|
| 21_언어 | 19 | **15** | 15 | ✅ 정확 일치 |
| exam_math | 20 | 20 | 20 | ✅ 동일 |
| exam_kor | 25 | 24 | ? | typeset 1쪽 적음 |
| exam_eng | 11 | 9 | ? | typeset 2쪽 적음 |

TypesetEngine은 verification 모드만 동작 (실제 렌더링 미사용). dump-pages 실행 시 TYPESET_VERIFY stderr 메시지로 출력 중이었음.

### 검증
- `cargo test`: 992 passed, 0 failed
- 4샘플 페이지 수 무변화 (Paginator 기반 유지)
- 단별 측정 도구 정상 동작

### 상태
🔍 의외의 발견. 본 #312 범위 종료, 새 sub-issue 후보: "TypesetEngine을 main pagination으로 전환". 이 sub-issue 완료 시 Epic #309 클로즈 가능 추정.

## Task #313 — TypesetEngine을 main pagination으로 전환

### 변경 범위
- **코드**:
  - `src/document_core/queries/rendering.rs::paginate()` — Paginator → TypesetEngine default 전환, RHWP_USE_PAGINATOR=1 fallback. TYPESET_VERIFY 검증 코드 제거
  - `tests/hwpx_to_hwp_adapter.rs` — 회귀 3건 #[ignore] (어댑터 측 보강 별도 sub-issue 분리)
  - `tests/golden_svg/issue-147/aift-page3.svg` — 페이지 번호 마커 추가 갱신
- **문서**: 수행계획서·구현계획서·stage1·2-3·5 보고서·최종 보고서

### 핵심 결과 (Epic #309 핵심 목표 달성)
| 샘플 | 이전 (Paginator) | 현재 (TypesetEngine) | PDF |
|------|------------------|----------------------|-----|
| 21_언어 | 19 | **15** | 15 ✅ |
| exam_math | 20 | 20 | 20 ✅ |
| exam_kor | 25 | 24 | (미보유) |
| exam_eng | 11 | 9 | (미보유) |

### 검증
- `cargo test`: lib 992 passed, integration 모두 통과 (어댑터 회귀 3건 분리)
- 4샘플 SVG 시각 변화: 자연스러운 페이지 압축에 따른 변화만 관찰

### 격리된 회귀 (다음 sub-issue 후보)
- HWPX → 어댑터 → HWP 변환 시 hwpx-h-02 +1쪽
- 어댑터의 paragraph/line_seg 보존 정확도가 TypesetEngine 의 line_seg 의존성과 맞지 않음

### 상태
✅ 핵심 완료. Epic #309 클로즈 가능 평가 (어댑터 회귀는 Epic 외 사안). 작업지시자 결정 사항.

## Task #314 — HWPX 어댑터 페이지 수 차이 보정 (부분 완료)

### 변경 범위
- **코드**: `src/document_core/commands/document.rs::normalize_hwpx_paragraphs` 추가 (HWPX 로드 후 char_shapes default + control_mask 재계산)
- **문서**: 수행계획서·구현계획서·stage1·2 보고서·최종 보고서

### 핵심 결과 (부분)
- LINE_SEG 100% 보존 확인
- char_shapes_len 차이 59건 + control_mask 차이 27건 normalize로 해소
- 페이지 수 +1쪽 차이 잔존 (origin 미식별)
- 격리된 3개 테스트 #[ignore] 유지

### 검증
- cargo test 회귀 0
- 4샘플 (21_언어 등) 페이지 수 무변화

### 상태
🔍 부분 완료. normalize 코드는 보존 (IR 정합성 가치). 잔존 origin (char_count, raw_break_type, 직렬화 손실 등)은 별도 sub-issue 후보.

## Task #317 — HWPX 어댑터 +1쪽 잔존 origin 식별 + 보강

### 변경 범위
- **코드**: `src/document_core/converters/hwpx_to_hwp.rs::adapt_table` — raw_ctrl_data 합성 시 attr 영역(offset 0..4)을 0으로 강제 + `table.attr=0` 보존. 기존 attr 합성 동기화 블록 제거.
- **테스트**: `tests/hwpx_to_hwp_adapter.rs` `#[ignore]` 3건 제거 (#313 격리분 회수)
- **문서**: 수행계획서 · 구현계획서 · stage1~4 보고서 · 최종 보고서

### 핵심 발견 (origin)
typeset 엔진의 `is_tac` 판정 (`table.attr & 0x01`) 이 `common.treat_as_char` 와 일치하지 않는 비대칭 구조. HWPX 직접 로드 시 `table.attr=0` 으로 block 분기 (PDF baseline 일치), 어댑터가 `pack_common_attr_bits` 로 비트 합성하면서 RELOADED 만 TAC 분기로 흘려 보내 표 paragraph 당 ~2.7px 누적 → pi=51 다음 페이지로 밀려 +1쪽.

### 검증
- `cargo test --test hwpx_to_hwp_adapter`: 25 passed, 0 failed, 0 ignored (3건 재활성화)
- `cargo test`: lib 992 + 통합 모두 PASS
- 4샘플 (21_언어/exam_math/exam_kor/exam_eng): 15/20/24/9 무변화 ✓

### 상태
✅ 완료. 격리된 어댑터 테스트 재활성화 + hwpx-h-02 변환 후 9쪽. merge 대기.

## Task #318 — 분할 표 + wrap=Square 호스트 문단 인라인 수식 중복 emit 회귀 수정

### 변경 범위
- **코드 (2 파일)**:
  - `src/renderer/layout/table_partial.rs` — Equation 분기에 `already_rendered_inline` 가드 (#301 동일 패턴)
  - `src/renderer/layout.rs` — PartialParagraph 분기에 `is_wrap_host` 가드 (FullParagraph 동일 패턴)
- **테스트**: `tests/issue_301.rs` `#[ignore]` 제거 (task314 커밋 `78fb1f1` 의 임시 처리 회수)
- **문서**: 수행계획서 · 구현계획서 · stage1~4 보고서 · 최종 보고서

### 핵심 발견 (두 origin)
1. `table_partial.rs:766` Equation 분기에 #301 가드 미적용 → z-table 셀 0.1915/0.3413/0.4332 가 각 2회
2. `layout.rs::layout_column_item` PartialParagraph 분기에 `is_wrap_host` 가드 누락 (FullParagraph 에는 있음) → wrap=Square 표 호스트 문단이 PartialParagraph PageItem + `layout_wrap_around_paras` 두 경로에서 중복 렌더 → 0.4772 body 1회 → 2회

### 검증
- `cargo test --test issue_301`: 1 passed (ignore 제거 후 통과)
- `cargo test`: 992 lib + 25 어댑터(0 ignored) + 통합 모두 PASS
- 4샘플 (21_언어/exam_math/exam_kor/exam_eng): 15/20/24/9 무변화 ✓

### 상태
✅ 완료. issue_301 정식 통과. merge 대기.

## Task #291 — KTX.hwp 2단 구성 TAC 표 좌측 밀림 (메인테이너 핀셋)

### 배경
어제 PR #273 검토 중 발견한 회귀 (#291). KTX.hwp 1페이지 2단 구성에서 오른쪽 단의 TAC 표 (pi=31, pi=32) 가 단의 좌측 끝에 붙어 렌더됨. 한컴 PDF 는 단의 우측 정렬.

### 사전 조사 결과
정적 SVG 와 WASM Canvas 모두 동일 증상 (이슈 등록 시 추정과 다름).

### 근본 원인
`src/renderer/layout.rs::layout_columns` 의 TAC 표 분기에 ParaShape `alignment` 반영 누락.
- 비-TAC + Square wrap 분기는 Task #295 (PR #298) 에서 `horz_align` 반영 추가됨
- **TAC 분기는 ParaShape `alignment` 무시** → align=Right/Center 인 TAC 표가 단 좌측에 강제 정렬

KTX.hwp pi=31 (TAC, ParaShape align=Right):
- 한컴 기대: x=518.56 (단 우측 정렬)
- rhwp: x=494.10 (단 좌측 강제)
- 차이: **24.46px (6.5mm)**

### 변경 범위
- **코드** `src/renderer/layout.rs:1991~2013` (~+15줄): TAC 분기에 alignment 매치 추가
  - Right → 단 우측 끝 - 표 폭 - margin_right
  - Center → 단 중앙
  - 기타 → 기존 base_x 유지
  - `.max(base_x)` 안전장치로 leading 보존

### 검증
- `cargo test --lib`: 992 passed (golden 무회귀)
- 5샘플 byte-diff:
  - KTX.hwp: 1/1 (의도)
  - exam_math: 0/20 (무영향)
  - 21_언어: 0/19 (무영향)
  - **aift.hwp: 18/74** (모두 의도된 개선 — Center/Right TAC)
  - biz_plan: 1/6 (의도된 개선)
- WASM Docker 빌드 + 브라우저 시각 검증: 작업지시자 판정 성공

### 산출물
- `mydocs/plans/task_m100_291{,_impl}.md`
- `mydocs/working/task_m100_291_stage{1,2,3}.md`
- `mydocs/report/task_m100_291_report.md`
- `mydocs/troubleshootings/tac_table_para_align.md` (재발 방지)

### 부가 성과
KTX 외 aift.hwp 18페이지 + biz_plan.hwp 1페이지 = 총 **20개 페이지의 TAC 표 정렬 개선** (제보 안 된 잠재 회귀 함께 해소).

### 교훈
1. **비대칭 패턴 식별** — 비-TAC Square wrap 분기에는 horz_align 처리가 있는데 TAC 분기에는 없는 것이 결정적 단서
2. **byte-diff 변경 ≠ 회귀** — 변경된 페이지의 align 패턴 분석으로 의도/회귀 판정. ParaShape align=Center/Right 인 TAC 표만 변경 → 모두 개선

### 후속 이슈 후보
- `t.common.horz_align` 와 ParaShape `alignment` 우선순위 정립

## Task #279 — 목차 right tab 리더 점 + 페이지번호 정렬 (PR #282 인수 마무리)

### 배경
외부 기여자 [@seanshin](https://github.com/seanshin) 이 PR #282 로 본 task 분석 + 핵심 2가지 수정 (리더 도트 dasharray + RIGHT tab 클램핑 제외) 제출. devel 충돌 + 범위 외 파일 다수 + 24h+ 응답 부재로 메인테이너 인수 결정.

### 인수 작업
1. PR 302 reopen + 작성자 fork force-push 합의 코멘트
2. `local/task279` 브랜치 (origin/devel 기준) 에 작성자 핵심 3 커밋 cherry-pick (author=hyoun mouk shin 보존)
3. 메인테이너 강화 수행계획서·구현계획서 추가 (Co-Authored-By: hyoun mouk shin)
4. 단계별 시각 검증 사이클로 6가지 추가 결함 식별·보강

### 메인테이너 추가 보강 (6가지)
1. trailing 공백 \t 케이스 (`\t `) 가드 정밀화 — cross-run RIGHT 진입 활성화
2. `resolve_last_tab_pending` 시그니처 확장 (fill_type 추가)
3. 리더 시멘틱 — leader 있는 RIGHT 탭은 cell inner 우측 끝으로 강제 (셀 padding 인지)
4. 페이지번호 폭별 leader 길이 차등화 — leader-bearing TextRun 검색 + leader.end_x 단축
5. 공백 only run carry-over — 정렬 단위 아님
6. trim_start 제거 — 선행 공백 시각 보정 (장제목 두 자리 케이스)

### 검증
- 992 lib + 6/6 svg_snapshot + 1 issue_301 + clippy + wasm32 통과
- 7 핵심 샘플 페이지 수 무변화
- KTX 목차 모든 페이지번호 right edge ≈ 700.0 으로 정렬 통일
- 작업지시자 WASM 시각 검증 통과

### 산출물
- `mydocs/plans/task_m100_279{,_impl}.md`
- `mydocs/working/task_m100_279_stage{1,2,3}.md`
- `mydocs/report/task_m100_279_report.md`
- `mydocs/troubleshootings/toc_leader_right_tab_alignment.md`
- `mydocs/tech/hwp_spec_errata.md` (entry 30 추가)
- `CHANGELOG.md` (Unreleased 섹션 추가)

### 기여 인정
1. cherry-pick author 보존 (3 커밋, hyoun mouk shin)
2. Co-Authored-By 체인 (메인테이너 신규 4 커밋)
3. CHANGELOG.md 항목 ("분석·구현 by [@seanshin]")
4. HWP Spec Errata entry 30 (발견·구현 본인 명기)
5. 트러블슈팅 문서 (외부 기여 인정 섹션)
6. PR / 이슈 close 코멘트
7. 최종 보고서 머리말 + 작성자 stage3 보고서 인용

### 교훈
**HWP 스펙 ≠ 한컴 조판 알고리즘**. 스펙 충실 구현으로는 한컴과 같은 결과가 나오지 않는다. 한컴 시각 결과를 정답으로 삼아 **시멘틱 재해석 + 자체 방어 로직** 으로 재구현. rhwp 는 "스펙 충실 구현체" 가 아니라 **"한컴 조판 결과를 재현하는 엔진"** 이어야 한다.

### 상태
✅ 완료 + 머지됨 (commit edcf361). PR #282 admin merge.

---

## Task #324 — form-002 page 1 인너 표 중복 노출 (PR #327)

### 배경
- 제보: `samples/hwpx/form-002.hwpx` SVG 의 page 1 하단에 "연구개발계획서 제출시…" 인너 표가 표출됨. PDF 기준 page 2 상단에 위치해야 함
- 외부 기여자 [@planet6897](https://github.com/planet6897) PR #327 제출

### 원인
- `compute_cell_line_ranges` 가 잔량(remaining) 기반 추적 → 잔량 0 도달 시 cumulative position 정보 손실
- 중첩 표(atomic) 문단의 가시성을 잘못된 heuristic 으로 판정 → page 1 에 무조건 표시
- `layout_partial_table` 의 split-end 행에서 (n,n) 마커 처리 분기 누락

### 변경 범위
- `src/renderer/layout/table_layout.rs::compute_cell_line_ranges` — cumulative position 기반 재작성 (v1)
- `src/renderer/layout/table_partial.rs` — split-end 행 atomic 스킵 + content_y_accum 갱신 (v2) + 통일 경로 (v3)
- `tests/golden_svg/form-002/page-0.svg` — 골든 갱신 (의도)

### 검증
- `cargo test --release`: 992 + 71 통과
- `cargo clippy --release -- -D warnings`: 클린
- form-002 page 1: 인너 표 제거 ✓
- form-002 page 2: 인너 표 정상 표출 ✓

### 산출물 (작성자)
- 수행계획서/구현계획서: `mydocs/plans/task_m100_324[_impl].md`
- Stage 1/2/2v2 보고서: `mydocs/working/task_m100_324_stage{1,2,2_v2}.md`
- 최종 보고서: `mydocs/report/task_m100_324_report.md`

### 상태
PR #327 머지 처리 중 (메인테이너 검토 + 시각 검증 + admin merge).
